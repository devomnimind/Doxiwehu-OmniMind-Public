[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "omniparser"
version = "0.1.0"
description = "Stub for OmniParser vision parser used by DevBrain V23"
authors = [{name = "DevBrain Team", email = "devbrain@example.com"}]
license = "MIT"
dependencies = ["numpy>=1.26.0"]
