# ============================================================================
# SECURITY HARDENED VERSIONS (Hugging Face Spaces Deployment)
# ============================================================================
# NOTE: Core OmniMind uses stricter versions in requirements-core.txt
# These versions address CVEs while maintaining HF Spaces compatibility
#
# Security Patches Applied:
# - transformers: Updated from 4.41.2 to fix ReDoS & deserialization CVEs
# - torch: Updated from 2.3.1 to address RCE in torch.load()
# - Other deps: Latest secure versions
# ============================================================================

fastapi>=0.122.0
uvicorn[standard]>=0.38.0
transformers>=4.57.0
torch>=2.9.0
accelerate>=0.30.0
python-dotenv>=1.2.0
pydantic>=2.12.0
